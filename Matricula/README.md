
  # Redesign applicant profile views

  This is a code bundle for Redesign applicant profile views. The original project is available at https://www.figma.com/design/40VLtVlNwpIUEq8YybopEE/Redesign-applicant-profile-views.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  